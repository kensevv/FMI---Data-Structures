#pragma once
#include <iostream>
#include <string>

int validateStringToInt(std::string str); // validates the input and converts string to int / returns -1 if invalid.
std::string toLower(const std::string& str); // returns all lower letters copy string.
void clearScreen(); // clears the console
